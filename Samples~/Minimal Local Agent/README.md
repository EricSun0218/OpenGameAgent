# Minimal Local Agent

1. Add `OpenGameAgentQuickstart` to an empty GameObject.
2. Enter Play Mode.
3. Check the Console for `OpenGameAgent sample completed`.

The sample deliberately uses a deterministic provider. It requires no API key, performs no network request, and demonstrates the same `GameInput -> GameAgentRuntime -> streamed model response` path used by a real provider.
