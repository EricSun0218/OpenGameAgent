{
	"name": "opengameagent",
	"version": "0.4.0-dev.0",
	"private": true,
	"description": "Agent runtime for AI-native game characters",
	"license": "MIT",
	"type": "module",
	"engines": {
		"node": ">=22.19.0"
	},
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "tsc -b",
		"clean": "tsc -b --clean",
		"check": "npm run format:check && npm run build && npm test",
		"format": "biome check --write package.json tsconfig.json tsconfig.base.json vitest.config.ts packages",
		"format:check": "biome check package.json tsconfig.json tsconfig.base.json vitest.config.ts packages",
		"test": "vitest run"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.5",
		"@types/node": "22.19.19",
		"typescript": "5.9.3",
		"vitest": "4.1.9"
	}
}
